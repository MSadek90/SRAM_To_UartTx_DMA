
#include <stdint.h>



char Array[] = "In a General-Purpose Computer running many processes simultaneously,\
		 we need a middle layer to manage the distribution of the computer’s hardware resources efficiently\
		 and fairly among all the various processes running on the computer.\
		 This middle layer is referred to as the kernel.\
		 The kernel virtualizes the computer’s common hardware resources to provide each process with its own virtual resources.\
		 This makes the process seem as if it is the sole process running on the machine.\
		 The kernel is also responsible for preventing and mitigating conflicts between different processes";


uint16_t Length = sizeof(Array);
